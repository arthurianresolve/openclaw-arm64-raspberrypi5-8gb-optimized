interface:
  display_name: "Excaliclaw Maintainer"
  short_description: "Maintain excaliclaw ports, tests, docs, and spec workflows"
  default_prompt: "Use $excaliclaw-maintainer to evaluate, spec when warranted, patch, validate, and hand off an arthurianresolve/excaliclaw change."

policy:
  allow_implicit_invocation: true
